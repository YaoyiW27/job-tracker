// Entry point for `npm run ingest`. The real fetch -> tag -> upsert pipeline
// lands in P0c (src/ingest/index.ts). This stub keeps the npm script wired.

async function main() {
  console.log("[ingest] pipeline arrives in P0c — nothing to do yet.");
}

main();
